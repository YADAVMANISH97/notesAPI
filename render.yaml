services:
  - type: web
    name: notesapi
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3000
      - key: CONNECTION_STRING
        sync: false
